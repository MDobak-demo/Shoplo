{% extends 'AppBundle::base.html.twig' %}

{% block body %}
    <h1>{{ product.name }}</h1>
    <h2>Price: {{ product.price.format(app.request.locale) }}</h2>
    <hr/>

    <p>{{ product.description }}</p>
{% endblock %}